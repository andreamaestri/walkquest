from django.urls import path
from . import views

app_name = "adventures"

urlpatterns = [
    # Empty for now - URL patterns will be added as needed
    # The API routes are already handled by the adventures router
]