from django.urls import path
# No need to export the router since it's already attached to the main API
# in the walks/api.py file

app_name = 'walks'

# Empty urlpatterns since all API endpoints are handled by the main API
urlpatterns = []
